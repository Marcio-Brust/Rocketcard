import React from "react";
import { ButtonCard } from "./Button.style";

export const Button = () => {
  function handleBackGround() {
    
  }

  return (
    <>
      <ButtonCard onClick={handleBackGround}>Gerar backgound</ButtonCard>
    </>
  );
};
